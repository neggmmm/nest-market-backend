version: "3.8"

services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    depends_on:
      - db


    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: